# GCS
GCS is a small back end worker instance. It has no conventional HTML front-end and can only be poked via its API.
The API uses the Laravel framework while the support code is written in a mix of standalone PHP, bash and C code.